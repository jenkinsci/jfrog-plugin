package integration.pipelines.declarative

pipeline {
    agent any
    tools {
        "JfrogInstallation" "cli"
    }
    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'jf -v'
            }
        }
     }
}

